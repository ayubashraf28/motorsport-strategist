# sim/src

Future home for engine-agnostic deterministic simulation packages.

Guidelines:
- keep logic pure and testable
- avoid rendering/UI dependencies
- model race state transitions explicitly

Note:
- v0 prototype implementation currently lives in `game/sim/src` for Godot runtime compatibility.

# tools/scripts

Developer automation scripts (non-CI) for repeatable local tasks.

Examples:
- bootstrap/setup scripts
- content/data generation helpers
- local validation helpers

Current scripts:
- `import_track.py`: converts racetrack CSV centerline data into V1.1 geometry JSON assets.

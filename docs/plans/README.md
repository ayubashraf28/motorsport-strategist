# docs/plans

Implementation plans for versioned feature development.

Each plan captures the *what* and *why* before implementation starts:
context, architecture decisions, file inventory, step sequence, and verification checklist.

Plans are committed before code changes so intent is traceable.

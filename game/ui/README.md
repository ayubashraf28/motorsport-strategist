# game/ui

UI layer for HUD, menus, overlays, and interaction components.

Requirements:
- responsive layouts
- accessible navigation and focus behavior
- consistent error/loading/empty states

Current v0 UI:
- `race_hud.tscn` + `race_hud.gd` for lap timing table and race controls
